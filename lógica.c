int jogar (ESTADO *estado, COORDENADA c){
    printf ("jogar %d %d\n",c.coluna ,c.linha);

    if ( estado ->tab[c.coluna][c.linha] = valido  (estado,[c.coluna][c.linha]){
     set_Branca_Tabuleiro(estado, [c.coluna][c.linha]);
    return 1;
    else return 0;
}
valido (ESTADO *e , COORDENADAS c)) {
    return 1 ;
}